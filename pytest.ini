[pytest]
testpaths = tests
markers =
    quality: LLM-judged output-quality tests (nondeterministic, needs a reachable Ollama instance) — excluded by default, run explicitly with `-m quality`
addopts = -m "not quality"
